import React, { Component } from 'react';
import { View, Text,  } from 'react-native';

class Login extends Component {
  render() {
    return (
      <View style={{backgroundColor: "#FFFFFF"}}>
        <Text style={{color: '#000000'}}> textInComponent </Text>
      </View>
    );
  }
}
export default Login;
